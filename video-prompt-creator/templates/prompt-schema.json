{
    "meta": {
        "topic": "{{topic}}",
        "theme": "{{theme}}",
        "style_preset": "{{style_name}}",
        "duration_seconds": 15,
        "aspect_ratio": "16:9",
        "fps": 24,
        "resolution": "4K",
        "pacing_strategy": "{{pacing_strategy}}",
        "camera_system": "{{camera_system}}",
        "lens_choice": "{{lens_choice}}",
        "film_stock": "{{film_stock}}"
    },
    "content": {
        "environment": {
            "description": "{{environment_detailed_description}}",
            "lighting_setup": "{{lighting_setup}}",
            "atmospherics": "{{atmospherics}}"
        },
        "character": {
            "description": "{{character_description}}",
            "skin_texture": "{{skin_texture_details}}",
            "wardrobe": "{{wardrobe_details}}",
            "fabric_physics": "{{fabric_physics}}"
        },
        "timeline": [
            {
                "time_range": "{{time_range_1}}",
                "type": "Establishing",
                "shot_type": "{{shot_type_1}}",
                "action": "{{action_segment_1}}",
                "camera_move": "{{camera_move_1}}",
                "focus": "Rack focus from {{focus_from}} to {{focus_to}}"
            },
            {
                "time_range": "{{time_range_2}}",
                "type": "Development",
                "shot_type": "{{shot_type_2}}",
                "action": "{{action_segment_2}}",
                "physics_interaction": "{{physics_detail}}",
                "vfx": "{{vfx_notes}}"
            },
            {
                "time_range": "{{time_range_3}}",
                "type": "Climax",
                "shot_type": "{{shot_type_3}}",
                "action": "{{action_segment_3}}",
                "camera_move": "{{camera_move_3}}",
                "resolution": "{{ending_state}}"
            }
        ]
    },
    "audio_config": {
        "ambient": "{{ambient_sound}}",
        "specific_sfx": "{{specific_sfx}}",
        "mood": "{{music_mood}}"
    }
}